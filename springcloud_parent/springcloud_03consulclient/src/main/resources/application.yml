server:
  port: 8083
spring:
  application:
    name: CONSULCLIENT
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        register-health-check: true